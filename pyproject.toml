[tool.poetry]
name = "compliment"
version = "0.1.0"
description = "Very simple compliment app"
authors = ["Max"]
readme = "README.md"
packages = [{ include = "src" }]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
